# JourneyMatch AI

- Backend: Python 3.11+, FastAPI, pytest.
- Frontend: React, Vite, npm.
- Keep recommendation behavior deterministic and privacy-first.
- Do not persist names, emails, payment data, loyalty numbers, or raw personal profiles.
- Run backend tests from `backend` with `pytest` and build the frontend with `npm run build` from `frontend`.
